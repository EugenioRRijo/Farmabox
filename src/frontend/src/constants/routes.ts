/**
 * Application routes
 * Centralized route definitions for future routing implementation
 */

export const ROUTES = {
  HOME: '/',
  SCHEDULE: '/schedule',
  PROFESSORS: '/professors',
  CLASSROOMS: '/classrooms',
  SUBJECTS: '/subjects',
} as const;
